# UPGRADE — Беклог

**Обновлено:** 2026-08-12

Живой список того, что реально осталось сделать по проекту, по итогам аудита git/рантайма/данных и работы над пайплайном сборки. В отличие от `docs/TODO.md` (устаревший, декабрь 2025, во многом про удалённый weather-модуль) — этот файл отражает текущее состояние.

---

## P0 — производительность / узкие места

Найдено при аудите бутылочных горлышек 2026-08-12. Структурные, требуют архитектурных решений, не быстрых патчей.

- [ ] **Глобальная сериализация: 1 RQ-воркер = 1 образец одновременно.** `worker_init.py` поднимает ровно один блокирующий `Worker.work()`, без `--burst`/пула/`replicas` в docker-compose. Сколько бы образцов ни стояло в очереди — обрабатывается строго по одному. При наблюдаемой длительности прогона 1.5–32+ часов это жёсткий потолок пропускной способности всей платформы.
- [ ] **Параллелить некуда физически.** Хост — 64 ядра CPU. `FLYE` в `nextflow.config` запрашивает `cpus = 60` на один образец — почти весь хост под один прогон. Добавить второй параллельный воркер сейчас нельзя, не пересмотрев резервирование ресурсов на процесс (Flye/Kraken2/CheckM и т.д.) — иначе будет драка за одни и те же ядра/память.
- [ ] **Хост реально перегружен соседями, не гипотеза.** `swap` **постоянно** на 100% (8Gi/8Gi), не разово. Чужие проекты на этом же сервере: QuestDB ~30ГБ, chatbot-backend ~20ГБ, vLLM ~7.5ГБ — стабильно, не пиково. Flye (до 120ГБ) и Kraken2 (100ГБ на загрузку базы) конкурируют за ту же физическую память.
- [x] **Непредсказуемая длительность прогона — причина найдена, это НЕ конкретная медленная стадия.** Исправление к прошлой записи: `nextflow_trace.txt` **уже** аплоадится в MinIO Silver (`{sample}/{pipeline_id}/nextflow_trace.txt`, код это делал всё это время — я просто раньше не проверил там, только на локальном диске). Скачал и разобрал реальный трейс для 32-часового `SRR30574959` (`genomic-silver/SRR30574959/823/nextflow_trace.txt`): сумма длительностей всех стадий — MEDAKA 2ч35м, FLYE 1ч9м, CheckM 15м и т.д. — **около 4-5 часов реального счёта**, но `submit` первой задачи (06:17 19 марта) и последней (14:20 20 марта) — **32 часа**. Между `CHECKM_METABAT2` (18:47 19 марта) и стартом `DREP`/`PROKKA` (14:15 **20** марта) — разрыв **~20 часов простоя**, не вычислений. Значит длительность определяет не тяжесть стадии, а **ожидание своей очереди** — напрямую подтверждает и усиливает пункт "1 воркер = 1 образец" выше, а не отдельная проблема.
- [ ] **Kraken2 (86ГБ `hash.k2d`) теперь грузится до 3 раз за один прогон** — `KRAKEN2_READS` (добавлен сегодня) + `KRAKEN2_METABAT2` + `KRAKEN2_CONCOCT`, каждый через `--memory-mapping` заново. Работает корректно, но лишняя нагрузка на I/O/память, которую не оптимизировал.
- [ ] **Арифметика беклога**: при пропускной способности "1 образец за раз" и разбросе 2–32ч, полная очистка текущего P1-беклога данных (см. ниже) может занять от нескольких дней до пары недель непрерывной работы.

## P1 — данные и повторные прогоны

- [ ] **`SRR32022268` — заблокирован повреждёнными исходными данными, не переретраивать без нового скачивания.** Ретрай 2026-08-12 (pipeline_id=1489) упал за 2 минуты на `NANOPLOT`: `ValueError: Lengths of sequence and quality values differs` — конкретный рид в fastq повреждён (длина seq ≠ длина quality). Нужна повторная закачка с SRA, вне скоупа сейчас.
- [x] **Побочная находка: `work_dir` переиспользуется между прогонами — исправлено.** В логе `SRR32022268` был одновременно `.fastq` и `.fastq.gz` для одного образца → NANOPLOT запускался дважды. Причина: `sample_work_dir` общий между попытками (для `-resume`), но `download_from_bronze()` качает только файлы текущего `pipeline_id` — старый файл от прошлой попытки не перезаписывался, просто соседствовал. Исправлено — `local_input_dir` теперь чистится перед каждой докачкой (коммит `9d41b3b5`). `rq-worker` перезапущен.
- [x] **125 исторических failed** (`ont_sra_50/`, `ont_sra_500/`) — пересабмичены через очередь 2026-08-12 (`pipeline_id` 1364–1488), данные уже были в MinIO Bronze. Батч занял 1ч46м (~85с/образец в среднем, быстрее прогноза). Итог: **123 completed, 2 failed**. Оба failed (`SRR22372228`, `SRR37301215`) — не баг: покрытие чуть выше порога 1.0x (1.02x / 2x), корректно ушли в ветку сборки вместо skip, но физически недостаточно ридов для Flye (2 и ~1700 ридов соответственно) — заранее предсказанный, принятый компромисс порога, не понижаю его.
- [ ] **21 "никогда не отправленных" образец — оказались пустыми директориями-заглушками.** Перепроверено 2026-08-12: все 21 (`ERR15817183`, `SRR18448245`, `SRR19443802` и т.д. — полный список в git-истории коммита) содержат **0 файлов** внутри `ont_sra_50/500/<sample>/`. Изначальная находка "24 образца, ни разу не отправленных" была основана только на существовании директории, без проверки содержимого — по факту отправлять нечего. Нужно заново скачивать с SRA/ENA, если данные вообще нужны (не в MinIO Bronze — проверено).
- [ ] **116 из 183 успешно собранных `completed`-образцов (63%) — ретро-классификация невозможна, только полный переретрай.** Проверено 2026-08-12: бины (`04_binning`/`05_drep`/`05_filtered`) в MinIO Silver/Gold не сохранились — только логи/отчёты, ни одного `.fa`. Скачал реальный `06_kraken2/*_kraken2_summary.tsv` для `SRR37301096` — подтверждает баг: все 13 бинов помечены `Classification failed, 0 reads`. Единственный путь — полный переретрай через пайплайн (сборка+биннинг+CheckM+dRep+Kraken2, не дешёвая skip-ветка), то есть часы на образец × 116 = реально многодневная задача при однопоточной очереди (см. P0). Нужно осознанное решение, не быстрый фикс.

## P2 — код и инфраструктура

- [ ] **`06_kraken2_reads/`** не подключён к `generate_summary.py` — отчёт по ридам лежит на диске (`results/<sample>/06_kraken2_reads/`), не попадает в JSON-сводку и дашборд.
- [x] **`UPGRADE_BASE_DIR` проверен и добавлен `web-backend`.** Тот же DooD-доступ (`/nextflow:ro` + `docker.sock`) и тот же код (`tasks/pipeline_tasks.py`) через общий bind-mount `/app`, но без переменной — латентный риск, если когда-нибудь появится прямой вызов `execute_nextflow_pipeline` в обход RQ. Добавлено (коммит `9121f0ae`), контейнер пересоздан. Прогонов Nextflow больше нигде в проекте не запускается (только batch-скрипты `scripts/data_processors/*` и тестовые `.sh`, которые уже используют свои собственные вызовы, не через web-backend/rq-worker).
- [ ] Read-based AMR по-прежнему невозможен (Abricate требует сборку контигов) — постоянное архитектурное ограничение, не баг, но стоит иметь в виду при планировании low-coverage образцов.
- [ ] **MetaSUB-миграция (`013_metasub_metadata.sql`) — только схема.** `models/sample.py` (`SampleBase`/`SampleCreate`) и `repositories/sample_repository.py` (явный 24-колоночный `INSERT`) ещё не знают о новых полях — записать их через живой `/api/pipeline/submit` пока нельзя, только напрямую SQL (как это уже делают batch-скрипты импорта).
- [ ] **`MetaSUB_metadata_protocol.pdf`** лежит в корне репозитория незакоммиченным — пользователь загрузил для сверки, не уточнили, нужно ли добавлять в git (например, в `docs/`).

## P3 — проверено (2026-08-12)

- [x] **GTDB-Tk** — лёгкая структурная проверка (`nextflow -preview` с dummy-путём к `gtdbtk_db`): ветка `GTDBTK_CLASSIFY` корректно активируется в DAG, без ошибок. Полный прогон с реальной базой (`/opt/gtdbtk_data/gtdbtk_r220_data.tar.gz`, 102ГБ в архиве) сознательно не делал — слишком дорого для структурной проверки, реальную базу распаковывать не стал.
- [x] **Нулевые риды после Filtlong** — прогнал реальный синтетический fastq (5 ридов по 50bp, Q2, ниже всех порогов Filtlong). `bases.txt` корректно даёт `0` (не падает), покрытие считается как `0`, образец уходит в skip-ветку, маркер и `KRAKEN2_READS` отрабатывают без крашей. `assembly_status` в summary.json корректно показывает `skipped_low_coverage` — но только когда тестовая структура папок соответствует реальной (`{sample_code}/input/*.fastq.gz`); при нестандартной раскладке эвристика `workflow.onComplete` для определения sample_id промахивается (не баг продакшена, артефакт тестовой раскладки, задокументировано на будущее).

## Вне нашего контроля

- Форк **Dubciuc/upgrade** на GitHub всё ещё хранит старую историю git с утёкшим паролём (`weather_pass_2024`, подтверждённо мёртвый). Это чужой репозиторий — можно только вручную предупредить владельца.

---

## Закрыто недавно (для контекста, не бэклог)

- ✅ Git: раздутие `.git` (9.2 ГБ → 58 МБ), утечка `.env` в публичной истории (история переписана и запушена), убраны секреты `.claude/settings*.json` из будущих коммитов.
- ✅ Postgres backup: чинился дважды — сначала рассинхрон пароля роли/`.env`, затем полноценно приведено к текущему `.env` с пересозданием `postgres`/`postgres_backup`.
- ✅ Баг в дашборде: `ResultsViewer` терял completed/failed прогоны при пагинации.
- ✅ Осиротевший контейнер `weather_consumer` — удалён.
- ✅ Пайплайн: добавлен coverage-gate перед Flye + read-based Kraken2 fallback (коммит `8d52c573`).
- ✅ Баг с `awk`, печатавшим большие суммы в экспоненциальной записи — чинился в рамках той же фичи.
- ✅ `UPGRADE_BASE_DIR` не был задан → Kraken2 в прод-прогонах монтировал пустую БД с середины февраля (коммит `75a4a023`). Подтверждено рабочим прогоном `SRR28272155`.
- ✅ Отсутствующая `/tmp/nextflow` ломала бутстрап Nextflow внутри `rq-worker` — создана вручную (не закоммичено, это состояние хоста, не репозитория).
- ✅ `MINIMAP2_MAPPING`/`PROKKA` — убраны такие же нерабочие `errorStrategy`-overrides, как были у `FLYE` (коммит `55c40f00`).
- ✅ **`SRR28272155`** (pipeline_id=1363) — успешно пересобран после 4 попыток, каждая упиралась в отдельный, теперь исправленный баг (awk-экспонента → `UPGRADE_BASE_DIR` → отсутствующая `/tmp/nextflow`).
- ✅ MetaSUB-протокол сверен со схемой БД, миграция `013_metasub_metadata.sql` написана и применена к живой базе (коммит `cabc929c`) — 14 новых полей, все constraint'ы проверены на реальных вставках, 997 существующих строк не тронуты. `sample_role` заведён отдельной колонкой, не трогая уже занятый под другое `sample_type`.
- ✅ `nextflow_executions` больше не зависает на `running` навсегда — добавлен `finalize_nextflow_execution()`, вызывается из всех 4 терминальных веток (успех/failure/timeout/exception) (коммит `da9dcfe4`). Проверено на живых данных: успех-ветка на реальной зависшей записи `SRR28272155`, failure-ветка на одноразовой тестовой строке. `rq-worker` перезапущен, чтобы будущие прогоны подхватили фикс.
