# UPGRADE — Беклог

**Обновлено:** 2026-08-11

Живой список того, что реально осталось сделать по проекту, по итогам аудита git/рантайма/данных и работы над пайплайном сборки. В отличие от `docs/TODO.md` (устаревший, декабрь 2025, во многом про удалённый weather-модуль) — этот файл отражает текущее состояние.

---

## В процессе

- **Ретрай `SRR28272155`** (pipeline_id=1363) — прогоняется через очередь с фиксами покрытия и `UPGRADE_BASE_DIR`. Три предыдущих попытки (1360–1362) падали на разных, уже устранённых багах (см. "Закрыто недавно").

---

## P1 — данные и повторные прогоны

- [ ] **`SRR32022268`** — второй отменённый крупный образец (`/data/large_samples`, ~5 ГБ). Ретраить после завершения `SRR28272155` (последовательно, не параллельно — сервер общий, память ограничена).
- [ ] **125 исторических failed** (`ont_sra_50/`, `ont_sra_500/`) — падали из-за низкого покрытия (<1x), теперь пайплайн их не роняет, а классифицирует по ридам. Задним числом не пересабмичены.
- [ ] **24 образца** — есть на диске (`ont_sra_50/500`), ни разу не отправлялись в пайплайн вообще.
- [ ] **116 из 183 успешно собранных `completed`-образцов (63%)** — остались без таксономии Kraken2 из-за бага с `UPGRADE_BASE_DIR` (см. "Закрыто недавно" — сам баг пофикшен, но задним числом данные не пересчитаны). Ретро-классификация возможна без пересборки, если бины ещё живы в MinIO Silver/Gold — нужно отдельно проверить.

## P2 — код и инфраструктура

- [ ] **`06_kraken2_reads/`** не подключён к `generate_summary.py` — отчёт по ридам лежит на диске (`results/<sample>/06_kraken2_reads/`), не попадает в JSON-сводку и дашборд.
- [ ] **`MINIMAP2_MAPPING` и `PROKKA`** — используют тот же паттерн динамического `errorStrategy`-closure, что был у `FLYE` и эмпирически не работал как задумано (не предотвращал падение всего прогона). У `FLYE` эту настройку убрали, у этих двух — не проверяли и не трогали.
- [ ] Проверить, не нужен ли `UPGRADE_BASE_DIR` ещё каким-то сервисам, кроме `rq-worker` (если Nextflow запускается ещё откуда-то).
- [ ] Read-based AMR по-прежнему невозможен (Abricate требует сборку контигов) — постоянное архитектурное ограничение, не баг, но стоит иметь в виду при планировании low-coverage образцов.

## P3 — не проверено эмпирически

- [ ] GTDB-Tk отключался (`--gtdbtk_db null`) в обоих ручных тестовых прогонах при разработке coverage-фичи — не подтверждено, что он всё ещё корректно встраивается в общий DAG.
- [ ] Не гонялся живой случай образца с **нулём** ридов после Filtlong (не просто мало, а вообще ноль) — код должен обработать корректно (coverage=0 → skip), но не проверено на реальных данных.

## Вне нашего контроля

- Форк **Dubciuc/upgrade** на GitHub всё ещё хранит старую историю git с утёкшим паролём (`weather_pass_2024`, подтверждённо мёртвый). Это чужой репозиторий — можно только вручную предупредить владельца.

---

## Закрыто недавно (для контекста, не бэклог)

- ✅ Git: раздутие `.git` (9.2 ГБ → 58 МБ), утечка `.env` в публичной истории (история переписана и запушена), убраны секреты `.claude/settings*.json` из будущих коммитов.
- ✅ Postgres backup: чинился дважды — сначала рассинхрон пароля роли/`.env`, затем полноценно приведено к текущему `.env` с пересозданием `postgres`/`postgres_backup`.
- ✅ Баг в дашборде: `ResultsViewer` терял completed/failed прогоны при пагинации.
- ✅ Осиротевший контейнер `weather_consumer` — удалён.
- ✅ Пайплайн: добавлен coverage-gate перед Flye + read-based Kraken2 fallback (коммит `8d52c573`).
- ✅ Баг с `awk`, печатавшим большие суммы в экспоненциальной записи — чинился в рамках той же фичи.
- ✅ `UPGRADE_BASE_DIR` не был задан → Kraken2 в прод-прогонах монтировал пустую БД с середины февраля (коммит `75a4a023`).
- ✅ Отсутствующая `/tmp/nextflow` ломала бутстрап Nextflow внутри `rq-worker` — создана вручную (не закоммичено, это состояние хоста, не репозитория).
